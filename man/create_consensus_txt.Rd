% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_consensus_txt.R
\name{create_consensus_txt}
\alias{create_consensus_txt}
\title{Create a consensus Familias TXT from replicate review Excel files}
\usage{
create_consensus_txt(
  reports_dir,
  output_txt = "consensus.txt",
  loci_order = default_loci_order(),
  max_alleles = 2,
  sheet = "Final_calls",
  pattern = "\\\\.xlsx$",
  value_columns = c("Haplotype_1", "Haplotype_2"),
  sample_id_prefix = "consensus_"
)
}
\arguments{
\item{reports_dir}{Directory containing the replicate review Excel files.}

\item{output_txt}{Output TXT filename or path. If relative, it is written inside
\code{reports_dir}. If \code{NULL}, the file is not written.}

\item{loci_order}{Character vector with the expected loci and output order.
Defaults to \code{default_loci_order()}.}

\item{max_alleles}{Maximum number of unique alleles allowed per locus.}

\item{sheet}{Excel sheet to read from each review file.}

\item{pattern}{File pattern used to find Excel files.}

\item{value_columns}{Two columns containing the haplotype values to combine.
Defaults to \code{Haplotype_1} and \code{Haplotype_2}.}

\item{sample_id_prefix}{Prefix used for the consensus sample id.}
}
\value{
A list with the consensus table, combined reports, conflict loci,
missing loci, input files and output path.
}
\description{
Reads several per-sample review Excel files generated by the package, combines
the haplotypes observed across replicates, and writes a one-row Familias-style TXT.
}
\examples{
\dontrun{
res <- create_consensus_txt(
  reports_dir = "path/to/replicate_review_excels"
)

res$consensus_txt
res$conflicts
res$missing_loci
}
}
